name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    
    steps:
    - uses: actions/checkout@v1
    - name: install package
      run: make install
    - name: Run linter
      run: make lint
    - name: node -v
      run: make node
